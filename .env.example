# Admin Authentication
ADMIN_USERNAME=admin
ADMIN_PASSWORD=adminpassword123

# Server Configuration
PORT=3000
NODE_ENV=development

# Data & Database Directory (Optional, defaults to .data/nimploy.db locally or /etc/nimploy in production)
# DATA_DIR=.data
