{
	"name": "nimploy",
	"private": true,
	"workspaces": [
		"apps/*"
	],
	"scripts": {
		"dev:server": "pnpm --filter=@nimploy/server run dev",
		"dev:client": "pnpm --filter=@nimploy/client run dev",
		"dev": "pnpm --filter=@nimploy/server run dev",
		"build": "pnpm --filter=@nimploy/client run build && pnpm --filter=@nimploy/server run build",
		"build:server": "pnpm --filter=@nimploy/server run build",
		"build:client": "pnpm --filter=@nimploy/client run build",
		"test": "pnpm --filter=@nimploy/server run test",
		"typecheck": "pnpm --filter=@nimploy/server run build",
		"db:generate": "pnpm --filter=@nimploy/server run db:generate",
		"db:migrate": "pnpm --filter=@nimploy/server run db:migrate",
		"format-and-lint": "biome check .",
		"check": "biome check --write --no-errors-on-unmatched --files-ignore-unknown=true",
		"format-and-lint:fix": "biome check . --write"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.7",
		"@types/node": "^24.4.0",
		"dotenv": "16.4.5",
		"lint-staged": "^15.5.2",
		"tsx": "4.22.4"
	},
	"packageManager": "pnpm@10.22.0",
	"engines": {
		"node": ">=20.0.0",
		"pnpm": ">=9.0.0"
	},
	"lint-staged": {
		"*": [
			"biome check --write --no-errors-on-unmatched --files-ignore-unknown=true"
		]
	},
	"pnpm": {
		"onlyBuiltDependencies": [
			"better-sqlite3",
			"esbuild"
		]
	}
}
